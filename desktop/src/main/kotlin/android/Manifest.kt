package android

/** Desktop shim for android.Manifest: permission names (every permission is granted on desktop). */
object Manifest {
    object permission {
        const val POST_NOTIFICATIONS = "android.permission.POST_NOTIFICATIONS"
    }
}
