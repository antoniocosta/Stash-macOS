package android.content.pm

/** Desktop shim for android.content.pm.ActivityInfo (package + class name of the resolved handler). */
open class ActivityInfo(@JvmField var packageName: String, @JvmField var name: String)
